apiVersion: apps/v1 
kind: Deployment 
metadata:
  name: wedoo
spec:
  replicas: 4
  selector:
    matchLabels:
      app: wedoo-app
  template:
    metadata:
      labels:
        app: wedoo-app
    spec:
      containers:
      - name: wedoo-app
        image: registry.gitlab.com/blablabla123-cell/we_do_osm_flutter/test:fa90c095f035117e6b3cdd9fd5e27c83602166a3
        ports:
        - containerPort: 3000
      imagePullSecrets:
      - name: gitlab-credentials
       
